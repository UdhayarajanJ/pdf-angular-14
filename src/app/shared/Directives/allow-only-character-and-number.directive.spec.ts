import { AllowOnlyCharacterAndNumberDirective } from './allow-only-character-and-number.directive';

describe('AllowOnlyCharacterAndNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowOnlyCharacterAndNumberDirective();
    expect(directive).toBeTruthy();
  });
});
