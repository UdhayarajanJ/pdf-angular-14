import { AllowOnlyCharacterDirective } from './allow-only-character.directive';

describe('AllowOnlyCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowOnlyCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
